diff --git a/node_modules/@kitware/vtk.js/Rendering/OpenGL/RenderWindow.js b/node_modules/@kitware/vtk.js/Rendering/OpenGL/RenderWindow.js
index 1a97a95..a4a1110 100644
--- a/node_modules/@kitware/vtk.js/Rendering/OpenGL/RenderWindow.js
+++ b/node_modules/@kitware/vtk.js/Rendering/OpenGL/RenderWindow.js
@@ -651,8 +651,15 @@ function vtkOpenGLRenderWindow(publicAPI, model) {
     return ret;
   };
   function clearEvents() {
-    model.canvas.removeEventListener('webglcontextlost', _preventDefault);
-    model.canvas.removeEventListener('webglcontextrestored', publicAPI.restoreContext);
+    // [2026-08-12 修复] 加空值保护。vtkOffscreenMultiRenderWindow 销毁时会先把
+    // model.canvas 置 null 以打破 canvas→WebGLContext 引用链（GPU 残留修复），
+    // 导致此处 removeEventListener 读到 undefined 抛异常，进而使
+    // RenderingEngine.destroy() 中途中断、引擎无法从 renderingEngineCache 注销，
+    // WebGL 上下文常驻 GPU 内存。加 if 保护后 delete 不再抛错，引擎可正常释放。
+    if (model.canvas) {
+      model.canvas.removeEventListener('webglcontextlost', _preventDefault);
+      model.canvas.removeEventListener('webglcontextrestored', publicAPI.restoreContext);
+    }
   }
   publicAPI.delete = macro.chain(() => {
     if (model.context) {
diff --git a/node_modules/@kitware/vtk.js/Rendering/OpenGL/Texture.js b/node_modules/@kitware/vtk.js/Rendering/OpenGL/Texture.js
index 0b6a6e2..fd0c59f 100644
--- a/node_modules/@kitware/vtk.js/Rendering/OpenGL/Texture.js
+++ b/node_modules/@kitware/vtk.js/Rendering/OpenGL/Texture.js
@@ -6,7 +6,6 @@ import vtkDataArray from '../../Common/Core/DataArray.js';
 import { Y as isPowerOfTwo, T as nearestPowerOfTwo } from '../../Common/Core/Math/index.js';
 import vtkViewNode from '../SceneGraph/ViewNode.js';
 import { registerOverride } from './ViewNodeFactory.js';
-import supportsNorm16LinearCached from './Texture/supportsNorm16Linear.js';
 
 const {
   Wrap,
@@ -186,9 +185,14 @@ function vtkOpenGLTexture(publicAPI, model) {
     }
   };
   const getNorm16Ext = () => {
-    if ((model.minificationFilter === Filter.LINEAR || model.magnificationFilter === Filter.LINEAR) && !supportsNorm16LinearCached()) {
-      return undefined;
-    }
+    // [2026-08-12 修复] 直接信任 Cornerstone 已设置好的 oglNorm16Ext。
+    // 原因：Cornerstone 在 init.js 的 _hasNorm16TextureSupport() 中已通过
+    // getSupportedTextureFormats() 同时验证 norm16 和 norm16Linear 支持，
+    // 仅当两者都为 true 时才设置 canUseNorm16Texture=true，并据此设置 oglNorm16Ext。
+    // 此处 vtk.js 内部的 supportsNorm16LinearCached() 是冗余的二次检测，可能因
+    // 时序/独立 canvas 差异误判为 false，导致 getNorm16Ext 返回 undefined，
+    // 使 SHORT 数据回退到 RGBA32F(4字节/像素)，GPU 显存翻倍(7-8G)。
+    // 直接返回已设置的 oglNorm16Ext（null 时自然回退到 float），保证 norm16 生效。
     return model.oglNorm16Ext;
   };
 
diff --git a/node_modules/@kitware/vtk.js/Rendering/WebGPU/RenderWindow.js b/node_modules/@kitware/vtk.js/Rendering/WebGPU/RenderWindow.js
index aeb4236..d5f8270 100644
--- a/node_modules/@kitware/vtk.js/Rendering/WebGPU/RenderWindow.js
+++ b/node_modules/@kitware/vtk.js/Rendering/WebGPU/RenderWindow.js
@@ -119,15 +119,24 @@ function vtkWebGPURenderWindow(publicAPI, model) {
     if (!model.initializing) {
       model.initializing = true;
       if (!navigator.gpu) {
+        // [2026-09-14 修复] WebGPU 不可用时恢复初始化状态，避免 GrowCut 打点分割空指针崩溃后卡住。
         vtkErrorMacro('WebGPU is not enabled.');
+        model.initializing = false;
         return;
       }
-      publicAPI.create3DContextAsync().then(() => {
+      publicAPI.create3DContextAsync().then(initializedContext => {
+        if (!initializedContext) {
+          model.initializing = false;
+          return;
+        }
         model.initialized = true;
         if (model.deleted) {
           return;
         }
         publicAPI.invokeInitialized();
+      }).catch(error => {
+        model.initializing = false;
+        vtkErrorMacro(`WebGPU initialization failed: ${error?.message || error}`);
       });
     }
   };
@@ -180,20 +189,38 @@ function vtkWebGPURenderWindow(publicAPI, model) {
       powerPreference: 'high-performance'
     });
     if (model.deleted) {
-      return;
+      return false;
+    }
+    if (!model.adapter) {
+      // [2026-09-14 修复] 浏览器/驱动拒绝 WebGPU adapter 时不要继续读取 requestDevice。
+      vtkErrorMacro('WebGPU adapter is not available.');
+      return false;
     }
     // console.log([...model.adapter.features]);
     model.device = vtkWebGPUDevice.newInstance();
-    model.device.initialize(await model.adapter.requestDevice());
+    const gpuDevice = await model.adapter.requestDevice();
     if (model.deleted) {
       model.device = null;
-      return;
+      gpuDevice?.destroy?.();
+      return false;
     }
+    if (!gpuDevice) {
+      vtkErrorMacro('WebGPU device is not available.');
+      model.device = null;
+      return false;
+    }
+    model.device.initialize(gpuDevice);
     // model.device.getHandle().lost.then((info) => {
     //   console.log(`${info.message}`);
     //   publicAPI.releaseGraphicsResources();
     // });
     model.context = model.canvas.getContext('webgpu');
+    if (!model.context) {
+      vtkErrorMacro('WebGPU canvas context is not available.');
+      model.device = null;
+      return false;
+    }
+    return true;
   };
   publicAPI.releaseGraphicsResources = () => {
     const rp = vtkRenderPass.newInstance();
