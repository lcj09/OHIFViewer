diff --git a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/RegionSegmentPlusTool.js b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/RegionSegmentPlusTool.js
index b18cf1d..6006985 100644
--- a/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/RegionSegmentPlusTool.js
+++ b/node_modules/@cornerstonejs/tools/dist/esm/tools/annotation/RegionSegmentPlusTool.js
@@ -38,34 +38,47 @@ class RegionSegmentPlusTool extends GrowCutBaseTool {
         }, this.configuration.mouseStabilityDelay || 500);
     }
     async onMouseStable(evt, worldPoint, element) {
-        await super.preMouseDownCallback(evt);
-        const refVolume = cache.getVolume(this.growCutData.segmentation.referencedVolumeId);
-        const seeds = calculateGrowCutSeeds(refVolume, worldPoint, {}) || {
-            positiveSeedIndices: new Set(),
-            negativeSeedIndices: new Set(),
-        };
-        const { positiveSeedIndices, negativeSeedIndices } = seeds;
-        let cursor;
-        if (positiveSeedIndices.size / negativeSeedIndices.size > 20 ||
-            negativeSeedIndices.size < 30) {
-            cursor = 'not-allowed';
-            this.allowedToProceed = false;
-        }
-        else {
-            cursor = 'copy';
-            this.allowedToProceed = true;
+        let enabledElement;
+        try {
+            // [2026-09-14 修复] 悬停预览阶段没有活动分割或点越界时不抛全局错误，避免分割后再次点击弹框。
+            await super.preMouseDownCallback(evt);
+            const refVolume = cache.getVolume(this.growCutData.segmentation.referencedVolumeId);
+            const seeds = calculateGrowCutSeeds(refVolume, worldPoint, {}) || {
+                positiveSeedIndices: new Set(),
+                negativeSeedIndices: new Set(),
+            };
+            const { positiveSeedIndices, negativeSeedIndices } = seeds;
+            let cursor;
+            if (positiveSeedIndices.size / negativeSeedIndices.size > 20 ||
+                negativeSeedIndices.size < 30) {
+                cursor = 'not-allowed';
+                this.allowedToProceed = false;
+            }
+            else {
+                cursor = 'copy';
+                this.allowedToProceed = true;
+            }
+            enabledElement = getEnabledElement(element);
+            if (element) {
+                element.style.cursor = cursor;
+                requestAnimationFrame(() => {
+                    if (element.style.cursor !== cursor) {
+                        element.style.cursor = cursor;
+                    }
+                });
+            }
+            if (this.allowedToProceed) {
+                this.seeds = seeds;
+            }
         }
-        const enabledElement = getEnabledElement(element);
-        if (element) {
-            element.style.cursor = cursor;
-            requestAnimationFrame(() => {
-                if (element.style.cursor !== cursor) {
-                    element.style.cursor = cursor;
-                }
-            });
-        }
-        if (this.allowedToProceed) {
-            this.seeds = seeds;
+        catch (error) {
+            this.allowedToProceed = false;
+            this.seeds = null;
+            this.growCutData = null;
+            if (element) {
+                element.style.cursor = 'default';
+            }
+            return;
         }
         if (enabledElement && enabledElement.viewport) {
             enabledElement.viewport.render();
@@ -87,22 +100,36 @@ class RegionSegmentPlusTool extends GrowCutBaseTool {
             });
         }
         const { world: worldPoint } = currentPoints;
-        await super.preMouseDownCallback(evt);
-        this.growCutData = csUtils.deepMerge(this.growCutData, {
-            worldPoint,
-            islandRemoval: {
+        try {
+            // [2026-09-14 修复] 打点分割异步失败时吞掉受控错误并恢复光标，避免对比模式点击后报 TypeError。
+            await super.preMouseDownCallback(evt);
+            this.growCutData = csUtils.deepMerge(this.growCutData, {
+                worldPoint,
+                islandRemoval: {
+                    worldIslandPoints: [worldPoint],
+                },
+            });
+            this.growCutData.worldPoint = worldPoint;
+            this.growCutData.islandRemoval = {
                 worldIslandPoints: [worldPoint],
-            },
-        });
-        this.growCutData.worldPoint = worldPoint;
-        this.growCutData.islandRemoval = {
-            worldIslandPoints: [worldPoint],
-        };
-        await this.runGrowCut();
-        if (element) {
-            element.style.cursor = 'default';
+            };
+            await this.runGrowCut();
+            this.allowedToProceed = false;
+            this.seeds = null;
+            return true;
+        }
+        catch (error) {
+            console.warn('RegionSegmentPlusTool: GrowCut failed.', error);
+            this.allowedToProceed = false;
+            this.seeds = null;
+            this.growCutData = null;
+            return false;
+        }
+        finally {
+            if (element) {
+                element.style.cursor = 'default';
+            }
         }
-        return true;
     }
     getRemoveIslandData(growCutData) {
         const { worldPoint } = growCutData;
diff --git a/node_modules/@cornerstonejs/tools/dist/esm/utilities/segmentation/growCut/runGrowCut.js b/node_modules/@cornerstonejs/tools/dist/esm/utilities/segmentation/growCut/runGrowCut.js
index 307ca21..e6602dd 100644
--- a/node_modules/@cornerstonejs/tools/dist/esm/utilities/segmentation/growCut/runGrowCut.js
+++ b/node_modules/@cornerstonejs/tools/dist/esm/utilities/segmentation/growCut/runGrowCut.js
@@ -11,6 +11,11 @@ const DEFAULT_GROWCUT_OPTIONS = {
         threshold: 1e-4,
     },
 };
+const setGrowCutWebGPUStatus = (supported) => {
+    if (typeof window !== 'undefined') {
+        window.__cornerstoneGrowCutWebGPUSupported = supported;
+    }
+};
 async function runGrowCut(referenceVolumeId, labelmapVolumeId, options = DEFAULT_GROWCUT_OPTIONS) {
     const workGroupSize = [8, 8, 4];
     const { windowSize, maxProcessingTime } = Object.assign({}, DEFAULT_GROWCUT_OPTIONS, options);
@@ -34,8 +39,21 @@ async function runGrowCut(referenceVolumeId, labelmapVolumeId, options = DEFAULT
         maxStorageBufferBindingSize: WEBGPU_MEMORY_LIMIT,
         maxBufferSize: WEBGPU_MEMORY_LIMIT,
     };
-    const adapter = await navigator.gpu?.requestAdapter();
+    if (!navigator.gpu) {
+        // [2026-09-14 修复] GrowCut 依赖 WebGPU，先做能力检查，避免 requestDevice 空指针崩溃。
+        setGrowCutWebGPUStatus(false);
+        throw new Error('GrowCut requires WebGPU, but navigator.gpu is not available.');
+    }
+    if (typeof window !== 'undefined' && window.__cornerstoneGrowCutWebGPUSupported === false) {
+        throw new Error('GrowCut requires WebGPU, but no GPU adapter is available.');
+    }
+    const adapter = await navigator.gpu.requestAdapter();
+    if (!adapter) {
+        setGrowCutWebGPUStatus(false);
+        throw new Error('GrowCut requires WebGPU, but no GPU adapter is available.');
+    }
     const device = await adapter.requestDevice({ requiredLimits });
+    setGrowCutWebGPUStatus(true);
     const BUFFER_SIZE = volumePixelData.byteLength;
     const UPDATED_VOXELS_COUNTER_BUFFER_SIZE = numIterations * Uint32Array.BYTES_PER_ELEMENT;
     const BOUNDS_BUFFER_SIZE = 6 * Int32Array.BYTES_PER_ELEMENT;
